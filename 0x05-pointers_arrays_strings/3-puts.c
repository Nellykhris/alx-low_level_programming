#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 *_puts - Prints a string to standard output
 *@str: The string be printed to output
 *Return: null
 */
void _puts(char *str)
{
	puts(str);
}
